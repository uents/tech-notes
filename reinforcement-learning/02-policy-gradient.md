# 方策勾配法

教材: Sutton & Barto 第13章（方策勾配法）、OpenAI Spinning Up の "Intro to Policy Optimization"

## この章で説明できるようになること

- 価値ベース（Q 学習）と方策ベースの違いを説明できる
- 方策勾配の式が「何の期待値」なのかを説明できる
- ベースラインと advantage が、なぜ分散を下げるのかを説明できる
- GAE の λ が何を調整しているかを説明できる
- 合法手が可変長のゲームで、方策をどう表すかを説明できる

## 1. 方策を直接学ぶ

> [!NOTE]
> - 目的関数 J(θ)（方策 π_θ で対戦したときの期待リターン）
> - 価値ベースとの違い（Q の最大値を取るのではなく、手の確率そのものを動かす）
> - 確率的な方策の利点（不完全情報ゲーム、探索）

### 目的関数 J(θ)

<!-- パラメータ θ の方策で対戦したときの期待収益。1章の期待収益と同じものを、θ の関数として見る -->

### 価値ベースとの違い

<!-- Q の最大値を取るのではなく、手の確率そのものを動かす。合法手が可変長のときに効いてくる違い -->

### 確率的な方策の利点

<!-- 不完全情報ゲームで手を読まれないこと、探索が方策に組み込まれること -->

## 2. 方策勾配定理と REINFORCE

> [!NOTE]
> - 勾配の形（log π(a|s) の勾配 × リターン）の意味
> - なぜ「今の方策で集めた対局」が必要になるのか
> - REINFORCE の弱点（分散が大きい）

### 方策勾配定理

<!-- 勾配が、状態分布の微分を含まない形に書けること。何の期待値になっているか -->

### log π の勾配が出てくる理由

<!-- ∇π = π∇log π の変形。なぜサンプルからの平均で推定できる形になるのか -->

### REINFORCE の手順

<!-- 1エピソード集める → G_t を計算する → ∇log π(a|s) を G_t で重み付けして更新する -->

### 分散が大きい理由

<!-- G_t のばらつき（1章の「勝敗だけが報酬」の話と結びつける）。報酬が全部正のとき何が起きるか -->

## 3. ベースラインと advantage

> [!NOTE]
> - ベースライン b(s) を引いても偏りが出ない理由
> - advantage A(s, a) = Q(s, a) − V(s) の意味
> - critic（価値のネットワーク）の役割

### ベースラインを引く

<!-- G_t − b(s) で更新する。b(s) は行動に依存しないこと -->

### 偏りが出ない理由

<!-- ∇log π の期待値が 0 になること。式で確かめる -->

### advantage

<!-- A(s,a) = Q(s,a) − V(s)。「この局面の平均と比べて、この手がどうだったか」。A の符号で確率がどう動くか -->

### critic

<!-- V を学ぶネットワーク。actor（方策）との役割分担。1章の TD 学習が、ここで使われること -->

## 4. GAE（一般化 advantage 推定）

> [!NOTE]
> - 1ステップ TD と、エピソード全体のリターンの間を取る
> - λ の役割（偏りと分散のつり合い）
> - 24位の設定は λ = 0.99。5位は「1ターンに約10.8判断あるので λ = 0.95（このターンと相手の返しまで）が 1.0 より良い」と報告している

### TD 誤差の重み付き和

<!-- 1章の n-step リターンとの関係。GAE の定義式を書く -->

### λ の役割

<!-- λ = 0 と λ = 1 のそれぞれが何になるか。偏りと分散のどちらが増えるか -->

### γ と λ の違い

<!-- どちらも先の報酬の重みを変えるが、役割が違う。24位は γ = 1.0、λ = 0.99 -->

### 判断の数と λ

<!-- 1ターンに何回の判断があるかで、λ が「何手先まで見るか」に相当する長さが変わる。5位の報告を自分の言葉で説明する -->

## 5. エントロピー正則化

> [!NOTE]
> - 方策が早く決め打ちになるのを防ぐ
> - 係数を大きく／小さくすると何が起きるか
> - 24位の設定は 0.01

### 方策のエントロピー

<!-- 確率分布の広がり。合法手の数が局面ごとに変わると、エントロピーの取りうる範囲も変わること -->

### 目的関数への足し方

<!-- 損失にどう入るか。符号に注意する -->

### 係数の効き方

<!-- 大きすぎるとどうなるか、小さすぎるとどうなるか。学習の診断にどう使うか -->

## 6. 合法手が可変長のときの方策

> [!NOTE]
> - 固定の行動の表を持たず、合法手を1つずつ採点する（ポインタ型）
> - 合法でない手を確率0にする（マスク）
> - 複数枚を選ぶ判断は、1枚ずつ選んで STOP で終える（自己回帰）
> - 上位チームのほぼ全員がこの形だった（ptcg-abc の `survey.md` 3.1節）

### ポインタ型の方策

<!-- 合法手それぞれを特徴量にし、点数を付けて softmax を取る。行動の数が変わっても同じネットワークで扱える理由 -->

### マスク

<!-- 合法でない手の確率を 0 にする。log π を計算するときに気をつけること -->

### 自己回帰の複数選択

<!-- 1枚ずつ選び、STOP で終える。1回の判断が複数ステップに分かれること -->

### 学習と推論で揃えるもの

<!-- 合法手の順序、マスク、STOP の扱い。ずれると何が壊れるか（7章の検査につながる） -->

## 確認

- [ ] 方策勾配が、なぜ「今の方策で集めた対局」を前提にするかを説明できる
- [ ] advantage が負のとき、その手の確率はどう動くかを説明できる
- [ ] λ を 1.0 に近づけたときと 0 に近づけたときの違いを説明できる
- [ ] 合法手が3つの局面と30の局面を、同じネットワークでどう扱うかを説明できる

## 手を動かす

- CleanRL の `ppo.py` で、advantage と GAE を計算している箇所を読み、この章の式と対応づける
- 24位の設定（`src/agents/dragapult/ppo/config.json`）の `gae_lambda`、`entropy_coefficient`、`value_coefficient` を、この章の言葉で説明してみる

## メモ

<!-- 疑問、後で調べること -->

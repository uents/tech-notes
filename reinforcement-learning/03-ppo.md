# PPO

教材: OpenAI Spinning Up の PPO、"The 37 Implementation Details of Proximal Policy Optimization"（ICLR Blog Track）、CleanRL の `ppo.py`

## この章で説明できるようになること

- PPO が何を防ぐための手法かを説明できる
- 重要度比とクリップが、式のどこで効くかを説明できる
- 設定値（学習率、クリップ、エントロピー係数、エポック数など）が何を決めているかを説明できる
- 24位の設定を読み、なぜその値かを自分の言葉で言える

## 1. 動機

> [!NOTE]
> - 方策勾配で大きく更新すると、方策が壊れる
> - 「今の方策から離れすぎない範囲で、できるだけ良くする」という考え方
> - TRPO（信頼領域）との関係

### 大きく更新すると壊れる

<!-- 2章の方策勾配は、今の方策の近くでしか正しくない。歩幅が大きいと何が起きるか -->

### 信頼領域という考え方

<!-- 方策の変化量に制約を置く。TRPO が KL に制約を置くこと -->

### PPO の立ち位置

<!-- TRPO の制約を、クリップという簡単な形で近似する -->

## 2. 重要度比

> [!NOTE]
> - 比 r(θ) = 今の方策がその手を選ぶ確率 ÷ 集めたときに選んだ確率
> - 収集時の確率を保存しておく必要がある理由
> - 比が1から離れるとき、何が起きているか

### 重要度サンプリング

<!-- 別の分布から集めたサンプルで期待値を推定する。1章の期待値の式に、比がどう入るか -->

### 収集時の確率を保存する

<!-- 更新を進めると方策が変わるので、集めたときの確率が後から再現できないこと -->

### 比が1から離れるとき

<!-- 更新が進んで方策が動いた状態。推定のばらつきがどうなるか -->

## 3. クリップされた目的関数

> [!NOTE]
> - 目的関数の形と、min を取る意味
> - クリップの幅 ε（24位は0.2）
> - クリップが効いている割合（clip fraction）を見る意味

### 目的関数

<!-- 比 × advantage と、クリップした比 × advantage の min。式を書く -->

### min を取る意味

<!-- advantage が正のときと負のときで、どちら側が抑えられるか。図を描くと分かりやすい -->

### クリップの幅 ε

<!-- 大きくすると／小さくすると何が変わるか。24位は 0.2 -->

### clip fraction

<!-- クリップが効いた割合。この値が大きい／小さいとき、何が起きているか -->

## 4. 価値の学習

> [!NOTE]
> - 価値の損失と、その係数（24位は0.25）
> - 価値のクリップ（24位は0.2）
> - 分布として価値を予測する方法（24位は51区間の分布）

### 価値の損失

<!-- 何を教師にして V を学ぶか（1章の TD 学習、2章の GAE と結びつける）。係数の役割 -->

### 価値のクリップ

<!-- 価値の予測が1回の更新で動きすぎないようにする -->

### 分布として価値を予測する

<!-- 期待値ではなく分布を予測する。勝敗だけが報酬のとき、分布の形がどうなるか -->

## 5. 学習の回し方

> [!NOTE]
> - 収集 → 学習データの作成 → 更新、のサイクル
> - 同じデータを何周するか（エポック）とミニバッチ
> - KL を見ながら学習率を決める／更新を止める
> - 勾配の大きさの上限（24位は0.5）

### サイクル

<!-- 収集、学習データの作成、更新。1サイクルで方策が1回入れ替わること -->

### エポックとミニバッチ

<!-- 同じ収集データを何周し、何回に分けて更新するか。更新が進むほど比が1から離れること -->

### KL の見方

<!-- 近似 KL で、方策が収集時からどれだけ動いたかを測る。学習率を決めるのに使う（24位）か、更新を止めるのに使う（一般的な早期停止）か -->

### 勾配の大きさの上限

<!-- 勾配クリップ。ばらつきの大きい推定で、たまに出る大きな勾配をどう扱うか -->

## 6. 実装の細部（37 Implementation Details より）

> [!NOTE]
> - advantage の正規化
> - 観測や報酬のスケーリング
> - 学習率の減衰
> - 初期化（直交初期化、方策の出力の小さな初期化）
> - どれが効き、どれが効かないと報告されているか

### advantage の正規化

<!-- ミニバッチごとに平均0・分散1にする。何が安定するか -->

### 観測と報酬のスケーリング

<!-- 入力と報酬の大きさを揃える。勝敗だけが報酬の場合は何が要らなくなるか -->

### 学習率の減衰と初期化

<!-- 直交初期化、方策の出力層を小さく初期化する理由 -->

### 効くもの、効かないもの

<!-- 記事が「効く」と報告しているものと、そうでないもの。自分の場合にどれが当てはまるか -->

## 7. 24位の設定を読む

`src/agents/dragapult/ppo/config.json`（4デッキ共通）

| 設定 | 値 | 何を決めているか（自分の言葉で） |
|---|---|---|
| `gamma` | 1.0 | |
| `gae_lambda` | 0.99 | |
| `clip` | 0.2 | |
| `value_clip` | 0.2 | |
| `value_coefficient` | 0.25 | |
| `entropy_coefficient` | 0.01 | |
| `learning_rate` | 3e-5 | |
| `weight_decay` | 1e-4 | |
| `max_grad_norm` | 0.5 | |
| `epochs` | 1 | |
| `updates` | 128 | |
| `batch_size` | 1024 | |
| `kl_target` / `kl_tolerance` | 0.004 / 0.001 | |
| `temperature` | 1.0 | |

<!-- 表を埋める。分からない項目は、教材のどこを読めば分かるかを書く -->

## 確認

- [ ] 重要度比が2.0になっている手は、どういう状態かを説明できる
- [ ] クリップがあるのに学習率も小さくする理由を説明できる
- [ ] エポック数を1にすると、何が犠牲になり、何が守られるかを説明できる
- [ ] 収集時の確率の記録がずれると、何が壊れるかを説明できる

## 手を動かす

- 24位の `src/agents/dragapult/ppo/scripts/calibrate.py` を読み、学習率を KL でどう選んでいるかを追う
- ptcg-abc の `docs/research/reproduce/24-population-rl.md` の段階4で、1サイクルの更新回数と KL の推移を確かめる
- CleanRL の `ppo.py` と 24位の `src/agents/dragapult/ppo/train.py` を並べ、同じ処理がどこにあるかを対応づける

## メモ

<!-- 疑問、後で調べること -->

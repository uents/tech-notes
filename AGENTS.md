# AGENTS.md

## 目的

このリポジトリは、技術書や資料を読み解き、自分の言葉でまとめた学習ノートを記録する。
読み手は、書いた本人とコーディングエージェントの両方である。
リポジトリの概要は `README.md` で把握する。

## 構成

- 題材ごとにディレクトリを作る。
- 章に分けるノートは、`index.md` に目的・目次・教材・進め方を置き、章ごとに `01-xxx.md` のように番号を付けたファイルを作る。

## エージェントの役割

- 本文は、ノートの持ち主が自分の言葉で書く。
- エージェントが手伝うのは次のこと。
  - 章立てや雛形を作る
  - 教材のどこを読めばよいかを案内する
  - 書かれた内容の誤りや抜けを指摘する
  - 理解を確かめる問いを出す
  - 教科書の式と、実装のどのコードが対応するかを探す手がかりを出す
- 指示がなければ、ノートの内容を書き換えない。誤りを見つけたら、直す前に指摘する。

## 事実の確かめ方

- 記憶で書かず、一次資料（教材、論文、公開コード、公式ドキュメント）で確かめてから書く。
- 教材に書かれていることと、自分の解釈を分けて書く。
- 確かめられなかったことは、そう書く。数値や仕様を作り上げない。
- 出典を書く。本は書名と章、Web は URL、コードはリポジトリ名・コミット・ルートからの相対パス。

## ドキュメント

- 日本語で書き、1文1行にする。
- 用語は初出で英語も書く（例: 方策 policy）。
- 式は必要な範囲で書き、記号の意味を必ず併記する。
- 実際に読んだ範囲、まだ読んでいない範囲、未検証の推測を区別する。

## 外部リポジトリの参照

- 関連するリポジトリのファイルは、そのリポジトリのルートからの相対パスで書く。
- 共有が制限されたコードやデータを、このリポジトリに写さない。
- 認証情報、個人の絶対パスを書かない。

## Git

- push するのは、原則としてこのリポジトリ（tech-notes）だけにする。
- `main` に直接コミットせず、作業ブランチを切る（`.claude/skills/git-commit/`）。
- コミットは Conventional Commits 形式で、scope にはノートのディレクトリ名を使う（例: `docs(reinforcement-learning):`）。
- ノートと関係のないファイル（ローカルの設定、エディタの作業ファイル）を Git に入れない。
- `.gitignore` にはリポジトリ内の相対パスだけを書き、先頭の `/`、個人の絶対パス、`../` を含むパスを書かない。
